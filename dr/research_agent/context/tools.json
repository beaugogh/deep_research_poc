[
    {
      "name": "tavily_search_tool",
      "description": "Perform web searches to gather factual information from public sources.",
      "input_schema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query string"
          },
          "max_results": {
            "type": "number",
            "description": "Maximum number of search results",
            "minimum": 1,
            "maximum": 10
          }
        },
        "required": ["query"],
        "additionalProperties": false
      }
    }
  ]
